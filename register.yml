---
- hosts: webservers
  become: true
  tasks:
  - name: Install net-tools package
    yum: name=net-tools state=present
    register: reg_output
  - name: Display the register variable output
    debug:
      var: reg_output
